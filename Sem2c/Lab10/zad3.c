#include <stdio.h>
#include <string.h>
typedef struct {
    int ile[100];
    char c;
}str;
str *sorcik(char *napis,int len) {
    str *tab;
    return tab;
}
int main(void){
    char *napis = "Sometimes you will never know the value of a moment until it becomes a memory";
    int len = strlen(napis);
    return 0;	
}
